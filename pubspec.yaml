name: flutter_app_test
description: A new Flutter application.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # For Handling Cupertino Icons
  cupertino_icons: ^1.0.2

  #Navigation
  stacked_services: ^0.8.13


  #snackBar
  flash: ^2.0.1


  #State Management
  provider: ^5.0.0
  stacked: ^2.2.5

  # OTP Text Field UI
  flutter_otp_text_field: ^1.0.0
  pin_code_fields: 7.3.0

  # For Text Scaling
  flutter_screenutil: ^5.0.0




  # dependency injection
  get_it: ^7.2.0

  # UI
  google_fonts: ^2.1.0

  #http: Testing Mocking Objects
  mockito: ^5.0.14

  #Local Storage
  shared_preferences: ^2.0.7




dev_dependencies:
  test:
  flutter_test:
    sdk: flutter

  build_runner:
  stacked_generator:
  auto_route_generator:


test:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true


